description: Mix outputs and inputs interpolation with literals

inputs:
  prefix:
    type: string
    required: true

steps:
  - bash: echo "dynamic-suffix"
  - run: echo-input
    with:
      message: outputs.last
  - bash: echo "prefix=$PI_IN_PREFIX literal=unchanged"
