description: Installer that always fails (for testing error output)

install:
  test: "false"
  run: |
    echo "attempting installation..." >&2
    echo "critical error: dependency XYZ not found" >&2
    exit 1
