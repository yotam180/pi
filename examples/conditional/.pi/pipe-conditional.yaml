name: pipe-conditional
description: Pipe passes through a skipped step

steps:
  - bash: echo "hello world"
    pipe_to: next

  - bash: tr a-z A-Z
    pipe_to: next
    if: os.windows

  - bash: cat
