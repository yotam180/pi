description: Install the uv Python package manager

install:
  test: command -v uv >/dev/null 2>&1
  run: curl -LsSf https://astral.sh/uv/install.sh | sh
  version: uv --version 2>&1 | awk '{print $2}'
